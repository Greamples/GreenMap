{
  "schemaVersion": 1,
  "id": "greenmap",
  "version": "${version}",
  "name": "GreenMap",
  "description": "GreenMap mod",
  "authors": [
    "Greamples"
  ],
  "contact": {},
  "license": "All-Rights-Reserved",
  "environment": "client",
  "entrypoints": {
    "client": [
      "org.greamples.greenmap.client.GreenMapClient"
    ]
  },
  "mixins": [
    {
      "config": "greenmap.client.mixins.json",
      "environment": "client"
    }
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-language-kotlin": ">=${kotlin_loader_version}",
    "fabric-api": "*",
    "minecraft": "${minecraft_version}",
    "xaerominimap": "${xaerominimap}"
  }
}
